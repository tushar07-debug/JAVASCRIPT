
// DOM-->Document Object Model
document.getElementbyId('firstHEading')
document.getElementById('firstHeading').innerHTML = "<h1>TUSHAR</h1>"

// title.innerText and title.textContent
//  in innertext we get only the display content that is visisble on the scrreen only but in the innertextcontent we get the text that is also hide and present behind the screen 

// innerHTML
// in innerhtml we get html value if we had write any html we also get html 
// we get also tags whereas tag are not fit in innercontent and innertext 